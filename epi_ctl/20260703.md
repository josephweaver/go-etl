# Epistemic Control Audit - 2026-07-03

Original Session:
20260703

Current Review:
T

## Session Scope

This session covered the first implementation slices of the
`workflow-execution-persistence` epic:

```text
001 Database Bootstrap and Schema Versioning
002 Canonical JSON and SHA-256 Helpers
003 Core Execution Schema
004 Project and Workflow Persistence Methods
005 Workflow Run and Stage Plan Persistence Methods
006 Work Item and Queue Persistence Methods
007 Attempt Claim Transaction - running queued_at schema atom
007 Attempt Claim Transaction - claim contract atom
007 Attempt Claim Transaction - empty queue claim atom
007 Attempt Claim Transaction - successful claim transition atom
007 Attempt Claim Transaction - rollback conflict test atom
007 Attempt Claim Transaction - worker ID claim coverage atom
```

The implementation introduced a new `internal/persistence` package with a
controller-facing `OpenStore` boundary, a concrete `Store` type, and a
SQLite-backed database adapter that owns connection setup, foreign-key
enforcement, schema metadata creation, supported-version validation, and failure
on unsupported schema versions.

## Completed Slice

- Added the 001 slice charter.
- Added the first persistence store-opening boundary.
- Added SQLite schema metadata initialization with supported version `1`.
- Added tests for invalid config, empty database bootstrap, existing supported
  schema, unsupported newer schema, missing metadata on an existing schema,
  foreign-key enforcement, and failed validation preserving the invalid state.
- Added canonical JSON v1 and SHA-256 helper functions in a new
  `internal/fingerprint` package.
- Added tests for stable object-key ordering, list ordering, null versus
  missing, integer JSON numbers, decimal/exponent rejection, decimal strings,
  SHA-256 digest generation, and lowercase digest validation.
- Added the first workflow-execution schema tables behind the SQLite adapter,
  including projects, workflows, workflow instances, stages, work items,
  workers, attempts, current placement, and terminal outcome tables.
- Added tests for table creation, metadata-only development schema replacement,
  incomplete same-version schema rejection, foreign-key enforcement, JSON
  checks, work-item idempotency uniqueness, and `completed_work.skipped_parent_id`.
- Added `Store` methods for idempotent project/workflow upsert, lookup, and
  safe delete-if-unused behavior.
- Added tests for project/workflow upsert, conflict rejection, missing lookup,
  foreign-key-backed workflow insert failure, and safe delete guards.
- Added `Store` methods for idempotent workflow-run creation, run lookup,
  ordered stage-plan insertion, stage lookup, and active-run listing.
- Added tests for run/stage idempotency, conflict rejection, missing lookup,
  foreign-key-backed missing-parent failures, and terminal-run exclusion from
  active-run listing.
- Added `Store` methods for idempotent compiled work-item insertion, work-item
  lookup, queue insertion, queued-work listing, and stage work-status counts.
- Added tests for work-item idempotency, conflict rejection, missing lookup,
  queue idempotency, queued-at conflict rejection, missing-work foreign-key
  failure, deterministic queue order, and status counts reconstructed from
  placement/terminal tables.
- Added the first 007 Review Atom by storing `queued_at` on `running_work` so
  later claim transactions can preserve queue eligibility evidence.
- Added a schema test for `running_work.queued_at` and updated the direct
  running-work fixture affected by the new required column.
- Added claim request/result structs and validation for caller-supplied attempt
  ID, executor type, and start timestamp without adding the claim transaction.
- Added the `ClaimNextWork` method boundary with request validation and a
  distinguishable empty-queue no-work result.
- Added the successful `ClaimNextWork` transaction for one queued item,
  including attempt insertion, running placement insertion with copied
  `queued_at`, queue deletion, commit-before-return behavior, and deterministic
  tie-break coverage.
- Added the 008 terminal attempt transition slice, including copied queue/start
  timing into terminal rows, idempotent completion/failure reports, conflict
  rejection, and rollback coverage for failed completion insertion.
- Added the 009 stage completion transition slice, including derived
  work-evidence readiness checks, idempotent stage completion, conflict
  rejection, and atomic publication of caller-supplied ready work.
- Added the 011 restart reconstruction query slice, including running-work
  listings, running-attempt lookup, terminal-attempt listings for a run, and
  run-level work status counts derived from placement and terminal tables.
- Started the 012 controller integration cutover by adding a
  workflow-execution store handle to `Controller` and opening the new
  persistence store during live startup instead of opening the old attempt
  ledger as the main database.
- Added the 012b persistence-backed status read model, so `/status` uses
  persisted queued, running, and failed work counts when a workflow-execution
  store is configured while retaining the old in-memory fallback.
- Added the 012c persistence-backed raw work submission bridge, so `POST /work`
  can persist validated raw work into a synthetic raw-work run and queue row
  when a workflow-execution store is configured.
- Added the 012d persistence-backed work claim endpoint path, so `/work/next`
  claims persisted queued work through `Store.ClaimNextWork` and returns the
  stored worker payload as the existing worker response shape.
- Added rollback/conflict coverage proving a duplicate claim `attempt_id`
  failure leaves the selected work item queued and creates no running placement.
- Added claim coverage for an existing worker ID, proving worker identity is
  recorded on both attempt and running placement rows when supplied.

## Concepts Introduced

- A concrete `Store` type as the controller-facing persistence boundary.
- A database adapter boundary that contains SQLite-specific behavior.
- Store opening as the owner of schema validation and migration setup.
- Schema metadata as the first durable main-database contract.
- A distinction between controller lifecycle intent and raw SQL details.
- Deterministic JSON encoding through sorted object keys.
- Canonical JSON v1's integer-only numeric contract.
- Decimal values represented as schema-defined strings.
- SHA-256 hex validation as a small reusable contract.
- Core workflow-execution schema as DDL owned by the database adapter.
- Current placement tables separated from terminal outcome tables.
- Destructive replacement limited to the known metadata-only development schema.
- Store methods as task-oriented lifecycle/data operations rather than raw SQL
  access from controller code.
- Immutable source identity records that reject conflicting rewrites for an
  existing generated ID.
- Workflow run records as the durable submission boundary under an existing
  project/workflow source identity.
- Stage plans as ordered, immutable rows that can be retried idempotently when
  the submitted plan is identical.
- Active-run listing as a query over child stage states rather than a separate
  mutable run-status column.
- Logical work-item records separated from their current queue/running/terminal
  placement.
- Queue visibility as durable database state that can be reconstructed after
  restart without an in-memory queue authority.
- Status counts as derived data over placement and terminal tables, not a
  separate summary table.
- Queue timestamps as durable eligibility evidence rather than mutable
  scheduling metadata.
- Claim preparation as a schema-level Review Atom before adding the actual
  queue-to-running transaction.
- Claim request validation as a separate Review Atom from the claim
  transaction.
- Empty-queue claim behavior as a separate Review Atom from the successful
  queue-to-running transition.
- Successful claim as the first transaction that moves durable work placement
  from queued to running.
- Rollback tests as explicit evidence that failed claim transitions do not
  partially move work.
- Worker identity as optional claim metadata that must reference an existing
  worker row when supplied.

## Review Questions

1. Why should controller startup eventually call `OpenStore` instead of calling
   individual migration functions?
2. Why does the SQLite adapter reject an existing schema with no
   `schema_version` table?
3. What invariant does `PRAGMA foreign_keys = ON` protect once workflow
   execution tables are added?
4. What is the difference between `Store` as a concrete type and a broad
   repository interface?
5. Why does this slice avoid creating workflow, stage, work-item, and placement
   tables?
6. Why does canonical JSON sort object keys but preserve list order?
7. Why does canonical JSON v1 reject `1.5` and `1e3` as JSON numbers?
8. Why should decimal values be represented as schema-defined strings?
9. Why should fingerprint helpers live outside persistence and workflow
   packages?
10. Why does the schema use separate `queued_work`, `running_work`,
    `completed_work`, and `failed_work` tables instead of one mutable status
    column?
11. Why is destructive replacement limited to the metadata-only development
    schema?
12. What does `completed_work.skipped_parent_id` represent?
13. Why do project and workflow upserts reject conflicting values for an
    existing generated ID?
14. Why do delete-if-unused methods return a boolean instead of treating a
    missing row as an error?
15. Why is stage-plan insertion idempotent when every existing row matches?
16. Why should a run with no persisted stages still appear in the active-run
    list?
17. Why can active-run listing be derived from stage terminal states instead of
    a separate run status field?
18. Why does this slice persist `work_items` separately from `queued_work`?
19. Why is re-enqueueing an already queued item with the same `queued_at`
    idempotent?
20. Why should status counts be reconstructed from placement and terminal
    tables instead of stored in a mutable counter row?
21. Why should re-enqueueing an item with a different `queued_at` fail instead
    of silently updating or ignoring the timestamp?
22. Why must `running_work` preserve the original `queued_at` before the claim
    transaction is implemented?
23. Why should claim request validation be defined before the SQL transaction
    that uses it?
24. Why should an empty durable queue return a no-work result instead of an
    error?
25. Why must `ClaimNextWork` insert the attempt and running row before deleting
    the queued row and returning the payload?
26. Why should a duplicate `attempt_id` failure leave the selected work item in
    `queued_work`?
27. Why can `ClaimNextWork` accept an empty worker ID but require an existing
    worker row when a worker ID is supplied?

## Verification

Focused test:

```text
go test ./internal/persistence
go test ./internal/fingerprint
go test ./internal/persistence
go test ./internal/persistence
go test ./internal/persistence
go test ./internal/persistence
go test ./internal/persistence
go test ./internal/persistence
go test ./internal/persistence
go test ./internal/persistence
go test ./internal/persistence
```

Full test:

```text
go test ./...
```

Both passed during the session.

## 012e2 Implementation Review

Completed slice:

- Added reuse candidates to work assignments.
- Added skip metadata and worker-observed hashes to completion reports.
- Let the worker use `internal/fingerprint` for canonical JSON and SHA-256
  hashing.
- Made the demo worker skip when its current state matches a prior candidate.
- Made the summary worker hash its input file path, size, and content before
  deciding whether reuse is safe.
- Mapped persisted skipped completions to `completed_work.skipped_parent_id`.
- Added persisted assignment reuse candidates from prior completed attempts
  selected by `resolved_inputs_sha256` and exact worker payload match.

Concepts introduced:

- Worker-observed hashes are different from controller-computed hashes of the
  JSON report document.
- A skipped worker report is still a successful completion; it is not a failed
  or invisible local no-op.
- `skipped_parent_id` is lineage from the new terminal row to the prior
  completed attempt that justified reuse.
- Go structs should be normalized into JSON-shaped values before using the
  canonical JSON helper.
- `resolved_inputs_sha256 + worker_payload_json` is only a temporary stand-in
  for the future `input_hash + controller_sha + plugin_sha` reuse key.

Review questions:

1. Why is the worker the right process to decide whether local execution can be
   skipped?
2. Why does the worker still report `/work/complete` when it skips execution?
3. What is the difference between `output_sha256` and `output_json_sha256`?
4. Why is `skipped_parent_id` stored on the completed terminal record?
5. Why is the current candidate filter conservative but incomplete?

Verification:

```text
go test ./internal/model ./cmd/worker ./cmd/controller
go test ./...
```

Passed during the session.

## 012e Implementation Review

Completed slice:

- Added optional `attempt_id` to assigned work items and worker failure
  reports.
- Added `output_json`, `pre_state_json`, and `post_state_json` to completion
  reports.
- Made persisted `/work/next` return the controller-owned attempt ID created
  by `ClaimNextWork`.
- Made persisted `/work/complete` canonicalize worker evidence, compute hashes,
  and call `Store.CompleteAttempt`.
- Made persisted `/work/fail` require `attempt_id` and call
  `Store.FailAttempt`.
- Added optional `failed_at` to failure reports so duplicate persisted failure
  reports can be identical at the store boundary.
- Updated the worker demo and summary operations to return `WorkEvidence` so
  terminal reports contain discernible output and state evidence.

Concepts introduced:

- Optional transport fields preserve legacy JSON compatibility while allowing
  the persistence path to require stronger data.
- A worker should report the controller-owned `attempt_id`; generating a new
  attempt ID at completion time breaks the persisted attempt lifecycle.
- Workers report JSON evidence, while the controller computes canonical hashes
  before database writes.
- Returning `(value, error)` lets successful work carry evidence without mixing
  success data into error handling.
- Persistence-backed terminal handlers should not mutate the legacy in-memory
  `assigned` map.

Review questions:

1. Why does persisted `/work/next` attach `attempt_id` to the work item before
   returning it to the worker?
2. Why is worker-generated attempt ID fallback still acceptable for legacy
   in-memory mode but not for workflow-execution persistence?
3. Why does the controller compute hashes instead of trusting hashes supplied by
   the worker?
4. What does `WorkEvidence` add beyond knowing that the worker wrote a file?
5. Why should persisted `/work/complete` avoid taking `Controller.mu`?

Verification:

```text
go test ./internal/model ./cmd/worker ./cmd/controller
go test ./...
```

Both passed during the session.
