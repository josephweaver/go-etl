# Project State

Last updated: 2026-07-11

This is the concise current-state index for GOET. The pre-split root state file is preserved at [`docs/history/PROJECT_STATE_2026-07-07_pre-split.md`](docs/history/PROJECT_STATE_2026-07-07_pre-split.md).

## Current Pointers

- State map and ownership: [`docs/STATE_INDEX.md`](docs/STATE_INDEX.md)
- Current focus: [`docs/CURRENT_FOCUS.md`](docs/CURRENT_FOCUS.md)
- Implemented capabilities: [`docs/IMPLEMENTED_CAPABILITIES.md`](docs/IMPLEMENTED_CAPABILITIES.md)
- Architecture state: [`docs/ARCHITECTURE_STATE.md`](docs/ARCHITECTURE_STATE.md)
- Runtime runbook: [`docs/RUNTIME_RUNBOOK.md`](docs/RUNTIME_RUNBOOK.md)
- Test and smoke status: [`docs/TEST_AND_SMOKE_STATUS.md`](docs/TEST_AND_SMOKE_STATUS.md)
- Development governance: [`docs/DEVELOPMENT_GOVERNANCE.md`](docs/DEVELOPMENT_GOVERNANCE.md)

## Concept State

- GORC worker direct development execution: [`docs/concepts/gorc-worker-direct-execution/README.md`](docs/concepts/gorc-worker-direct-execution/README.md). OS-001, OS-002, and OS-003 are implemented pending OS-003 review: `worker execute --config PATH --work-item PATH [--source-bundle PATH] [--result PATH]` loads runtime-only config and one resolved `model.WorkItem`, supplies safe direct attempt/source bookkeeping when missing, invokes the existing `Worker.Run` dispatch without a direct-mode operation allow-list, preserves local data/artifact/staging/log behavior, and writes a local completed/failed result with a simple `0`/`1` process status. Production source acquisition remains controller-backed; direct mode uses a local-file provider and performs no work, source, observation, terminal-report, or heartbeat request. The repeatable Python fixture proves successful and failed subprocess execution, source extraction, required `GOET_*` variables, input/output JSON, artifact promotion, retained stdout/stderr, result evidence, and zero total sentinel HTTP requests with and without a configured controller URL. The runtime runbook documents local, container, Apptainer, and allocated-HPCC development usage.
- Data assets and materialized outputs: [`docs/concepts/data-assets-and-materialized-outputs/STATE.md`](docs/concepts/data-assets-and-materialized-outputs/STATE.md). 2026-07-11 operational update: controller-generated worker configs now propagate `gdrive_rclone` enablement, executable, and config-path settings from `execution_environment.runtime.settings`; the dedicated VM/HPCC runtime completed a real Google Drive `cache_data -> python_script` smoke for `Data/Field_Boundaries/field_segments_minmax_report.tsv`, materializing a 31,501-byte TSV into the HPCC worker cache with `provider_type=gdrive_rclone`. The same runtime also completed `run-b1a165ac170360c93b7e552f12e02bb6`, caching `Data/Field_Boundaries/ReleaseData.7z` as a 261,861,012-byte immutable `gdrive_rclone` source and extracting selected members `h18v07/WELD_h18v07_2010_field_segments` and `h18v07/WELD_h18v07_2010_field_segments.hdr` as 100,000,000-byte and 976-byte cache outputs. The HPCC GDAL worker SIF at `/mnt/scratch/weave151/etl/runtime/images/goetl-worker-gdal-os007.sif` was rebuilt from the current worker source with SHA-256 `148f95012c71e90cb8c757e996e4ccec0d3eadb1f0bc564c0db52721239b4c4a`; VM runtime config now binds only the runtime root, and `run-6bda4bf566c7c889f5d5e565eebc69b3` completed the ReleaseData smoke without the temporary `/goetl/goetl-worker` binary overlay.
  - 2026-07-11 publish update: `commit_data` can publish file artifacts to `gdrive_rclone` targets by `remote` plus `drive_path`, with one-at-a-time upload resource constraints per remote and published-asset evidence using `storage_scope=gdrive_rclone`. The VM/HPCC runtime completed `run-f62051cb8854b217619f5bcbc5aaee07`, creating `goetl-publish-smoke.zip` on HPCC, uploading it to `gdrive:Data/ETL/test/goetl-publish-smoke.zip`, and verifying the Drive copy contains `2000.txt`, `2001.txt`, and `2002.txt`. The deployed controller hash for the nested Python artifact-manifest hydration fix is `878fd0add78c0011fcb33a8bf8de657fa2a55fb641b3408026babd22c94fe7c0`; the current HPCC GDAL worker SIF hash is `c84b31d5b0a87562004b02d6734ba805cd340ed0eea3afc37b73d22b6a4f65c4`.
- Geospatial worker plugins: [`docs/concepts/geospatial-worker-plugins/README.md`](docs/concepts/geospatial-worker-plugins/README.md). Operational slices `002-geospatial-operation-contract`, `003-raster-info-and-bounding-boxes`, `004-reproject-and-align-raster`, `005-stack-aligned-rasters`, `006-raster-pair-value-counts`, `007-crop-by-polygons`, `008-polygonize-raster`, `009-aggregate-by-polygons`, and `010-cdl-yanroy-fixture-workflow-and-docs` are implemented. `010` adds a GDAL worker smoke fixture workflow (`containers/goetl-worker-gdal/test`) that runs raster metadata, grid validation, align-to-grid, pair counting, and summary CSV generation. 2026-07-11 runtime fix update: GDAL raster metadata now preserves CRS WKT when no CRS-level EPSG code is available, EPSG parsing avoids parameter/unit authority IDs, raster pair counting accepts Int32/UInt32 field rasters and Byte/UInt16 value rasters, and Slurm worker scheduling can emit a configured `#SBATCH --time` limit. The LandCore HPCC smoke still has an open grid-alignment issue caused by literal WKT comparison after GDAL CRS normalization.
- Dependency-aware workflows: [`docs/concepts/dependency-aware-workflows/STATE.md`](docs/concepts/dependency-aware-workflows/STATE.md)
- Controller worker execution framework: [`docs/concepts/controller-worker-execution-framework/README.md`](docs/concepts/controller-worker-execution-framework/README.md). `OS-001-one-by-one-worker-capacity-manager` is implemented: the controller evaluates durable queued/running/resource-blocked demand, records in-memory inflight worker-start reservations, starts at most one worker while unsaturated, confirms one reservation when a worker claims work, and reuses the configured execution environment or fallback local worker starter from controller launch configuration. `controller_config.worker_execution_pattern` also supports `null`, which keeps demand evaluation active while disabling worker process scheduling and inflight launch reservations. Worker-capacity evaluation now removes the temporary launch reservation and logs a skipped start when scheduling is enabled but `worker_target_environment` is not configured, avoiding phantom inflight capacity for manual-worker runs. Worker configs generated from a local `execution_environment` now carry absolute worker paths, can include `python_executable` and `max_asset_bytes`, and avoid recopying an unchanged local worker executable while a prior direct-process worker may still be running. `OS-002-landcore-workflow-admission-diagnostic-slice` is implemented: workflow admission failures now report the admission phase and underlying cause, and LandCore-shaped field-year-crop synthetic fan-out admission covers object accessors that bind scalar, path, and list parameters.
- Controller CareTaker: [`docs/concepts/controller-caretaker/README.md`](docs/concepts/controller-caretaker/README.md). `OS-001-worker-lifecycle-persistence-foundation` is implemented in persistence: SQLite schema version 6 adds `worker_sessions`, session ownership on attempts and running work, one-running-assignment-per-session enforcement, and `abandoned_work` history. `internal/persistence.Store` can register, heartbeat, read, list live/expired, and end worker sessions, and can read abandoned-attempt history. `OS-002-worker-registration-heartbeat-and-stop-protocol` is implemented: shared worker lifecycle JSON models exist, controller defaults/config validation cover `worker_heartbeat_interval=1m` and `worker_dead_after=5m`, `/workers/register`, `/workers/heartbeat`, and `/workers/stop` persist lifecycle state behind worker/admin authorization, worker-side lifecycle client methods register/heartbeat/stop with typed session state, `RunHeartbeat` provides cancellable heartbeat supervision with rejected-session and self-fence errors, the normal worker loop registers before claiming and stops on no work or worker error, and work claim/completion/failure requests carry active worker/session identity headers. `OS-003-assignment-fencing-abandonment-and-requeue` is in progress: claim now validates active, non-expired, non-busy worker sessions inside the persistence transaction before queue mutation, terminal completion/failure calls carry owner fields that persistence checks before inserting completed or failed records, expired-session recovery atomically marks sessions dead and recovers owned running work, stop-with-running-work recovery atomically marks sessions stopped, records abandonment, removes running assignments, and requeues work, and late owner completion/failure reports against abandoned attempts now return assignment ownership lost without stale terminal rows. Remaining deterministic race tests focus on heartbeat/expiry ordering.
- Canonical workflow, data, and configuration document model: [`docs/concepts/canonical-workflow-data-document-model/README.md`](docs/concepts/canonical-workflow-data-document-model/README.md). OS-001 through OS-020 are implemented. `internal/document` now has decode-only public document contracts for controller, project, workflow, and submission override envelopes; strict JSON/YAML source decoding into JSON-compatible values with duplicate-key, null, float, unsupported-tag, anchor, and alias rejection; and a reusable loader that converts ordinary variable maps into sorted typed variables under caller-selected `controller_config`, `project_config`, `workflow`, or `override` namespaces. Inline canonical project envelopes now load only their `variables` object into `project_config.*`, while the legacy inline project map shape is still accepted for existing admission behavior. Canonical `Workflow` documents with `id`, `variables`, `steps`, `fan_out`, `work`, and `source_manifest` now decode through `internal/document`, adapt into existing `internal/workflow` compiler structures, and are accepted by controller inline workflow admission; true legacy Go-shaped workflow wrappers still fall back to the existing decode path, but canonical-looking documents that include the legacy top-level `workflow` wrapper now fail with a focused migration error. Canonical data composition now recursively overlays `project.data`, `workflow.data`, and submission data overrides with later-layer precedence, list/scalar replacement, object/scalar mismatch rejection, null deletion rejection, path-and-layer diagnostics, and stable canonical JSON/YAML equivalence. Canonical data definitions now model `data.inputs` and `data.outputs`, named file roles, ordered `select` projections, asset parameters, physical provider bindings, archive selection generation, output targets, and compilation into existing `BoundDataAsset`/`BoundPublishTarget` contracts without changing workflow runtime dispatch. Parameterized asset instantiation now adds internal `fanout` and `asset` lifecycle namespaces, resolves exact asset parameter bindings from fan-out or workflow references, resolves selected file templates into concrete bound assets, and computes deterministic alias-independent asset keys that vary by tile and effective selection. Materialization scope modeling now carries explicit `shared` or `worker` scope through template and bound asset materialization contracts, resolves shared materialization domains from target environment IDs, treats `worker` as known but not implemented through a sentinel error, rejects unknown scopes, requires scope in canonical capability validation, and exposes scope/domain lookup identity keys. Explicit canonical `cache_data` steps now parse `step.data.materialize`, instantiate one selected asset per fan-out item, emit visible `cache_data` work using the existing worker payload and provider resource constraints, reject compute-style work parameters on cache operators, and validate duplicate explicit materializers without discovering hidden cache work from compute `data_assets`. Controller assignment hydration now matches shared data requirements to completed prior dependency or prior-stage materialized manifests by asset key and shared materialization domain, projects the completed physical manifest under each consumer binding alias, and fails before assignment when no producer, a wrong domain, or duplicate producer manifests are found. Worker-side data projections now add an explicit `data` variable namespace, convert materialized manifests into `data.<alias>.path` path-typed lists and `data.<alias>.files.<role>.path` path-typed named projections, resolve Python argument tokens through the variable resolver at the worker boundary, and reject bare alias tokens such as `${field_segments}`. Explicit canonical `commit_data` steps now parse `step.data.outputs`, require `work.type: commit_data`, require explicit `from.step` and `from.artifact` lineage plus a declared output target, compile visible commit work with existing publication payloads and target write resource constraints, and reject canonical compute-side `publish` parameters so canonical workflows do not create hidden publish work. OS-013 now has a JSON/YAML semantic-equivalence guard that compiles equivalent canonical workflows through visible `cache_data`, compute, and `commit_data` steps, normal stage compilation no longer invokes the legacy `data_assets` or `publish` compute-parameter scanners, canonical compute steps reject both legacy `data_assets` and `publish` planner parameters, the wrapper authoring template now references `data.<binding>.path[0]` instead of legacy `local_path`/`data_assets` parameter examples, the internal data-operator smoke now uses explicit cache and commit operator steps instead of hidden compute-side planner parameters, a repository JSON/YAML fixture sweep found no remaining checked-in legacy workflow fixture to migrate, and `go test ./...` passes. OS-014 adds a semantic expression-function boundary: public `$type`/`$call`/`$args` directives with `$ref` argument nodes load into `variable.FunctionCallExpression`, function names are namespaced separately from variable namespaces, the registry is immutable and empty by default, `$expr` remains unsupported, and no concrete built-ins are present yet. OS-015 adds just-in-time resolver evaluation for semantic function calls through a `ResolverConfig` registry, with normal reference/accessor argument resolution, expected result-type enforcement, sensitivity/provenance propagation, unknown-function diagnostics, and preserved cycle/depth safeguards. OS-016 adds `list.crossproduct(left, right)`, which requires two list arguments, returns deterministic left-major two-item list pairs, preserves item types and sensitivity, returns an empty list when either input is empty, and does not mutate inputs. OS-017 adds `list.zip(left, right)`, which requires two equal-length list arguments, returns same-index two-item list pairs in order, preserves heterogeneous item types, returns an empty list for two empty lists, and rejects length mismatch instead of truncating. OS-018 adds `list.flatten(items)`, which requires one list argument containing only list children, concatenates child items in order, preserves item types and sensitivity, keeps deeper nested lists intact, and rejects scalar children instead of coercing them. OS-019 adds `list.length(items)`, which requires one list argument, returns a typed integer top-level item count, returns zero for an empty list, rejects non-list arguments, and relies on the resolver function-call framework to propagate sensitivity/provenance from sensitive list arguments. OS-020 proves canonical JSON/YAML structured function-call variables can produce list fan-out inputs consumed through normal `${workflow.*[*]}` references, compile equivalent visible cache work, bind function-produced pair fields into data-asset parameters, produce deterministic asset keys from those bound values, and preserve ordinary literal-list fan-out behavior.
- Canonical JSON variable loading: [`docs/concepts/canonical-json-variable-loading/README.md`](docs/concepts/canonical-json-variable-loading/README.md). Placeholder concept for importing controller, project, workflow, and override JSON into implicit typed namespaces using canonical JSON value shapes.
- Resource-constrained work admission: [`docs/concepts/resource-constrained-work-admission/STATE.md`](docs/concepts/resource-constrained-work-admission/STATE.md)
- Workflow execution persistence: [`docs/concepts/workflow-execution-persistence/STATE.md`](docs/concepts/workflow-execution-persistence/STATE.md)
- Operational observability: [`docs/concepts/operational-observability/STATE.md`](docs/concepts/operational-observability/STATE.md)
- Sensitive variable propagation: [`docs/concepts/sensitive-variable-propagation/README.md`](docs/concepts/sensitive-variable-propagation/README.md). Operational slices `001-sensitive-metadata-and-safe-rendering`, `002-protected-reference-model`, `003-controller-envelope-and-persistence`, `004-worker-secret-resolver-and-redactor`, `005-trusted-go-workitem-sensitive-context`, `006-python-subprocess-secret-materialization`, `007-controlled-sink-redaction-tests`, and `008-credentialed-worker-fixture-smoke` are implemented. `009-concept-closure-and-doc-sync` is implemented. `internal/variable` variable declarations now accept `sensitive` and protected references, resolved values now carry sensitivity metadata plus safe redaction labels, and sensitivity propagates through references, aggregates, accessors, interpolation, and protected-reference declarations without exposing plaintext through default string or JSON rendering. Controller work-item persistence and `/work/next` assignment payloads now include a `goet/execution-envelope/v1` envelope that separates public parameter values from protected-reference metadata; sensitive plaintext work-item parameters and client-submitted sensitive plaintext run variables are rejected instead of being stored. The worker now has a `worker_env` protected-value resolver, redacted `SensitiveValue` formatting/JSON behavior, explicit plaintext access, and an attempt-local literal redactor for exact materialized secret strings. Trusted in-process Go worker dispatch now builds an `OperationContext` that separates public values from declared sensitive values, registers materialized plaintext with a redactor, and routes handler logging through a safe logger. Python subprocess execution can now materialize explicitly requested `worker_env` protected refs into env vars or restrictive attempt-local temp files, scrub captured stdout/stderr, reject `GOET_OUTPUT_JSON` containing exact materialized secrets before persistence, and remove temp secret files on success and failure. Sentinel tests now verify that the exact `goet-secret-sentinel-007-do-not-persist` value is absent from safe rendering, controller status JSON, worker failure errors, captured stdout/stderr, structured output persistence, and inspected persistence rows; artifact-content scanning remains documented as outside the phase-1 policy. A repeatable credentialed-worker fixture smoke runbook now verifies that `worker_env:GOET_FIXTURE_TOKEN` is resolved only in the worker process and that the raw `goet-fixture-secret-008-do-not-persist` value is absent from controller status, submission status, captured logs, worker output, controller logs, and SQLite persistence.
- Source-control resolution and cache: [`docs/concepts/source-control-resolution-and-cache/STATE.md`](docs/concepts/source-control-resolution-and-cache/STATE.md). Local source aliases such as `local:demo` now resolve from the configured controller repository cache under `local/sources/<alias>/files`; controller startup no longer hardcodes `local:demo` to the sibling `../go-etl-demo-project`. The controller `/workflow` endpoint accepts either source-reference project/workflow paths or inline project/workflow JSON documents and admits both through the controller repository cache before persistence. `goet submit` now embeds local `--project` and `--workflow` JSON files by default, includes local files declared by the workflow `source_manifest`, and sends source-reference paths instead when `--repo`/`--repository` is supplied.
- SSH refinement: [`docs/concepts/ssh-refinement/README.md`](docs/concepts/ssh-refinement/README.md). `OS-001-ssh-proxyjump-transport` is implemented: `SSHTransportConfig` accepts explicit `jump_hosts`, jump hosts can inherit the target identity or use hop-specific identity settings, each hop uses its own host-key policy, and `SSHTransport.Connect` builds the final target SSH client through the jump chain without invoking the external `ssh` command. Existing Exec, SFTP copy/list, filesystem helpers, runtime preparation, and Slurm submission continue to use the final target client, so gateway hosts are transit-only. `OS-002-reverse-controller-callback-tunnel` is implemented: `execution_environment.callback_tunnel` can bind a remote listener on the final SSH target or a selected jump host such as `jump_hosts[0]`, proxy callback traffic to the local controller, validate the worker-facing `controller_url`, keep the tunnel idempotently available across repeated worker-start preparation, and close the tunnel with the execution environment during controller cleanup. Preflight covers gateway-side HTTP reachability and, for `SlurmScheduler`, submits a tiny `sbatch --wait` compute job that uses `curl` to verify the worker-facing `/healthz` URL from a Slurm allocation. `RemoteProcessScheduler` can launch a short-lived worker process over the configured transport for local/no-domain `ssh_reverse` smoke tests where the reverse listener is reachable from an HPCC dev/login node but not from Slurm compute nodes. `OS-003-ssh-config-ergonomics-and-host-key-verification` is implemented: local `identity_file` and `known_hosts_file` values expand `~`, `$VAR`, and `${VAR}` before local file reads, `known_hosts` verification uses `golang.org/x/crypto/ssh/knownhosts`, jump hosts can inherit the target `known_hosts_file`, and `known_hosts` policy now requires an explicit or inherited file instead of falling back to insecure behavior.
- Secure network exposure of the GORC controller API: [`docs/concepts/secure-network-exposure-of-gorc-controller-api/README.md`](docs/concepts/secure-network-exposure-of-gorc-controller-api/README.md). `OS-001-controller-api-auth-contract` is implemented. `internal/controllerauth` now contains the first authentication contract model: it decodes a resolved `controller_config.authentication` object into disabled or bearer mode, validates phase-1 roles (`client`, `worker`, `admin`), rejects unknown authentication and credential fields, rejects duplicate credential IDs, and enforces exactly one token source declaration per credential. It also contains the pure controller route policy for `GET /healthz`, workflow submission, source bundles, submission status/logs, raw work submission, worker claim/complete/fail, controller status, observation logs, and shutdown. Credential material loading now reads `token_env` and `token_file` sources once, trims one trailing line ending, rejects empty and duplicate token material, rejects token files with group/other permissions when permission bits are available, stores only SHA-256 digests for matching, and avoids token contents in returned errors. Checked-in controller defaults now include disabled `controller_config.authentication` and `controller_config.controller_insecure_external_http_allowed = false`. Controller startup now resolves auth before opening the database, loads credential material once, rejects disabled auth unless both listen host and advertised `controller_url` are loopback, rejects external plain HTTP unless the explicit override is true, and wires the resolved auth mode, credential store, and route policy into controller middleware. `OS-002-controller-route-authorization-middleware` is implemented: bearer-mode requests pass through one route authorization boundary, `/healthz` remains public, unknown paths return `404`, protected routes reject missing or invalid credentials with `401`, wrong roles receive `403`, valid callers using unsupported methods receive `405`, disabled-auth development mode bypasses all route checks after OS-001 loopback startup validation, and authenticated principals are attached to request context for handlers that need them later. `OS-003-shared-controller-http-client` is implemented: `internal/controllerhttp` provides a reusable controller HTTP client with deterministic loopback URL validation, same-origin redirect policy, safe path construction, bearer token injection through a redacting `SensitiveToken`, bounded sanitized status errors that close unexpected-status bodies, and no dependency on CLI, worker, SSH, Slurm, or workflow semantics. `OS-004-cli-controller-credential-loading` is implemented: the CLI can load controller tokens from `--controller-token-file`, `GOET_CONTROLLER_TOKEN_FILE`, or `GOET_CONTROLLER_TOKEN`, trims one trailing line ending, rejects empty or oversized tokens, rejects broadly readable token files on Unix-like systems, migrates CLI controller requests through `internal/controllerhttp`, uses public `/healthz` for reachability, and avoids local controller auto-start for non-loopback controller URLs. `OS-005-worker-control-plane-credential-bootstrap` is implemented: worker runtime settings accept `controller_token_file`, generated worker JSON carries only that file path, external worker controller URLs require a token-file path during preparation, execution-environment preflight verifies the configured file through the local filesystem or primary runtime transport without reading token contents into controller config, and worker config loading resolves the same path while requiring it for HTTPS or non-loopback HTTP controller URLs. `OS-006-worker-controller-http-client-migration` is implemented: worker startup builds one shared controller client from the configured worker token file, work claim/completion/failure, source-bundle downloads, and log-observation delivery now use `internal/controllerhttp`, and the remaining package-level worker `http.Get` is for external data assets rather than controller communication. `OS-007-laptop-test-https-ingress` is implemented: the repo now has a test-only laptop HTTPS ingress runbook, PowerShell helpers to render a local controller URL override and validate public ingress/auth behavior, and a placeholder bearer-authenticated laptop-test controller config. `OS-008-dedicated-server-https-deployment` is implemented: the repo now has provider-neutral dedicated-server HTTPS deployment guidance, example Caddy and systemd files, and a PowerShell endpoint smoke script that checks public health, protected status authentication, optional HTTP redirect behavior, and loopback-only protected-route behavior without printing tokens. `OS-009-network-security-and-external-smoke` is implemented: focused route-role and HTTPS client tests pass, the dedicated VM serves the controller through trusted HTTPS while keeping `8080` private, an external worker completed one HTTPS callback smoke, `singularity_worker` now defaults an omitted bind to `<runtime root>:<runtime root>`, a current `goetl-worker.sif` is staged for HPCC, and the dedicated VM successfully scheduled an actual HPCC Slurm/Singularity worker that claimed and completed `os009-hpcc-worker-001` through the public HTTPS endpoint. `OS-010-concept-closure-and-doc-sync` is in progress because the laptop-hosted temporary HTTPS smoke profile remains documented but not separately verified.
- 2026-07-10 OS-010 update: the local/no-domain `ssh_reverse` path now has dev-node `remote_process` evidence and real HPCC Slurm/Singularity evidence through a controller-managed dev-node relay. Workers using that relay opt into `controller_insecure_external_http_allowed` because their worker-facing URL is plain HTTP on a non-loopback host protected by the SSH reverse tunnel, relay locality, and worker bearer token.
