# Epistemic Control Audit - 2026-06-25

## Slice

Mode:

```text
EC-4 / feature / file(1)+test+doc+newfile
```

Objective:

Start a fake-HPCC Slurm container path that includes SingularityCE 4.1.2.

## Completed

- Created a branch for the slice: `feature/fake-hpcc-singularity-ce`.
- Added a Dockerfile that derives from the existing Dockerized Slurm image and installs SingularityCE 4.1.2.
- Added a WSL/Docker test script that builds the image and verifies `singularity --version`.
- Moved container build assets under the root `containers/` directory.
- Added a Docker worker image that compiles `./cmd/worker` into `/goetl/goetl-worker`.
- Added a worker image smoke test that runs the container and verifies the worker executable starts.
- Added `LocalTransport` and `DirectProcessScheduler` as local execution-environment components.
- Added tests showing local/direct components can be selected through `ExecutionEnvironmentConfig`.
- Added `SingularityWorkerRuntime` as a worker runtime wrapper that launches the worker through `singularity exec`.
- Added tests for the local/direct/Singularity runtime chain and controller worker submission command shape.
- Added a local Singularity controller config and workflow fixture.
- Verified a real worker runs through `LocalTransport -> DirectProcessScheduler -> SingularityWorkerRuntime`.
- Renamed the scheduler script variable to neutral `worker_script_path` while retaining `docker_slurm_script_path` compatibility.
- Refactored worker launch resolution to prefer structured `worker_config.transport`, `worker_config.scheduler`, and `worker_config.runtime` objects.
- Renamed the generic launch resolver files from `docker_slurm_worker*` to `worker_launch_config*`.
- Deleted the legacy `DockerSlurmWorkerStarter` and `DefaultWorkerStarter` path after confirming the controller uses `ExecutionEnvironment`.
- Documented the Jammy target versus Rocky Linux 9 local Slurm base-image constraint.
- Added typed resolver access helpers and moved generic worker launch variable retrieval into `internal/variable`.

## Concepts Introduced

- Docker can remain the local outer runtime while SingularityCE becomes the inner HPCC-like runtime.
- The current local Slurm image is Rocky Linux 9, so it cannot install the Ubuntu Jammy `.deb` directly.
- The local test image replaces the inherited Apptainer package with SingularityCE because the packages conflict.
- A Docker worker image can be the build/development source for the later SingularityCE worker runtime.
- The current worker has no `--help` mode, so the smoke test uses a missing config path as a narrow executable-start check.
- `LocalTransport` satisfies the same `Transport` interface as the Docker transport, but copies and executes on the local machine.
- `DirectProcessScheduler` satisfies the same `Scheduler` interface as Slurm, but starts the worker executable directly.
- `SingularityWorkerRuntime` is not a scheduler; it rewrites the worker process command while leaving capacity acquisition to the scheduler.
- Direct-process jobs must outlive the HTTP request context after successful submission, matching the Slurm job boundary.
- Demo ledgers can cause skip behavior; reset the local demo ledger when the runtime directory is intentionally cleared.
- Runtime variables should avoid backend-specific names when the concept is shared by local, Slurm, and Singularity paths.
- Layer-specific settings belong in the layer's `worker_config` object; `worker_env` should stay reserved for system environment variables.
- File names should follow current ownership boundaries; Docker/Slurm-specific names should not hold generic worker launch resolution.
- The current controller worker-start path is `ExecutionEnvironment`; older `WorkerStarter` adapters should not stay once they are no longer called by production code.
- Required and optional typed resolver methods keep missing-variable behavior separate from wrong-type errors.

## Review Questions

1. Why does the fake-HPCC image install the EL9 RPM instead of the Jammy `.deb`?
2. What role does Docker play in this local test if the target HPCC runtime is SingularityCE?
3. Why is replacing Apptainer acceptable in this fake-HPCC image?
4. What would need to change to make the local base image match the HPCC Jammy package exactly?
5. Why is the worker image kept separate from the Slurm plus SingularityCE fake-HPCC image?
6. Why is `DirectProcessScheduler` a scheduler in the controller model even though it does not queue work through Slurm?
7. Why does `SingularityWorkerRuntime` leave `WorkerConfigPath` as the scheduler-appended final argument instead of embedding it inside the runtime wrapper?
8. Why did the local Singularity demo need to clear its ledger before rerunning after deleting `/tmp/goetl-local-singularity`?
9. Why is `worker_script_path` a better controller variable name than `docker_slurm_script_path`?
10. Which launch settings belong in `worker_config.scheduler` versus `worker_config.runtime`?
